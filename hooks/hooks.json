{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh hooks/scripts/on-session-start.sh"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'if [ -n \"${KILN_TEAMS_ACTIVE:-}\" ]; then exit 0; fi; worktree_root=\"${KILN_WORKTREE_ROOT:-/tmp}\"; cwd=\"${PWD:-$(pwd)}\"; case \"$cwd\" in \"${worktree_root%/}/kiln-\"*) exit 0 ;; esac; sh hooks/scripts/on-task-completed.sh'"
          }
        ]
      }
    ]
  }
}
