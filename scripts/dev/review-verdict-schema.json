{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string" },
          "class": { "type": "string", "enum": ["BLOCKING", "ADVISORY"] },
          "remedy_class": { "type": "string", "enum": ["VERBATIM", "EXACT_CHECK", "EXISTING_RECEIPT", "NEW_DECISION"] },
          "location": { "type": "string" },
          "defect": { "type": "string" },
          "evidence": { "type": "string" }
        },
        "required": ["id", "class", "remedy_class", "location", "defect", "evidence"]
      }
    },
    "verdict": { "type": "string", "enum": ["APPROVED", "REJECTED"] },
    "notes": { "type": "string" }
  },
  "required": ["findings", "verdict", "notes"]
}
